.venv/
.venv
.fetchlinks/
# Everything mutable on a deployed host: catalog snapshot, collector state,
# batch spool, logs, runtime config and publisher.env. It lives inside the
# checkout so the whole install is one directory, and is ignored so that
# `git pull` can never disturb queued batches or a cursor.
/runtime/
.cache/
.npm/
**/.cache/
**/.npm/
**/_cacache/
node_modules/
**/node_modules/
data/logs/
**/data/logs/
ingest/data/logs/
fetch_links.log
*.log
*.log.*
# db/ now holds the checked-in SQL schema, so ignore database files by
# extension rather than ignoring the directory.*.db
*.db-wal
*.db-shm
*.sqlite
*.sqlite3
.vscode/
.idea/
__pycache__/
*.py[cod]
links.txt
REVIEW_PLAN.md
PLAN.md
